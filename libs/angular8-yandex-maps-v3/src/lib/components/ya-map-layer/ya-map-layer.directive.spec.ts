import { YaMapLayerDirective } from './ya-map-layer.directive';

describe('YaMapLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new YaMapLayerDirective();
    expect(directive).toBeTruthy();
  });
});
