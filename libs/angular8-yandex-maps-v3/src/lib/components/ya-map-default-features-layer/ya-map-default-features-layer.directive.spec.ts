import { YaMapDefaultFeaturesLayerDirective } from './ya-map-default-features-layer.directive';

describe('YaMapDefaultFeaturesLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new YaMapDefaultFeaturesLayerDirective();
    expect(directive).toBeTruthy();
  });
});
