import { YaMapDefaultSchemeLayerDirective } from './ya-map-default-scheme-layer.directive';

describe('YaMapDefaultSchemeLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new YaMapDefaultSchemeLayerDirective();
    expect(directive).toBeTruthy();
  });
});
