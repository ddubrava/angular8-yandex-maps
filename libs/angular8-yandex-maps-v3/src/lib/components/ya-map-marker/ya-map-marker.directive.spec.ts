import { YaMapMarkerDirective } from './ya-map-marker.directive';

describe('YaMapMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new YaMapMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
