import { YaMapDefaultMarkerDirective } from './ya-map-default-marker.directive';

describe('YaMapDefaultMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new YaMapDefaultMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
